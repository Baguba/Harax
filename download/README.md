# Harax downloads

- **harax.zip** — the complete Harax platform source, ready to run.

## How to run it

1. Unzip the file.
2. Requirements: Node.js 20+ (with npm).
3. In the unzipped `harax/` folder:

```bash
npm install
npm run dev
```

4. Open http://localhost:3000

Demo login: `selam.awoke@gmail.com` / `harax2026` (Student) — or use the 1-click demo chips in the sign-in modal. Full instructions, scripts and troubleshooting are in `README.md` inside the zip.
